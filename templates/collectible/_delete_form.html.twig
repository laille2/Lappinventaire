<form method="post" action="{{ path('collectible_delete', {'id': collectible.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collectible.id) }}">
    <button class="btn">Delete</button>
</form>
