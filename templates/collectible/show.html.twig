{% extends 'base.html.twig' %}

{% block title %}Collectible{% endblock %}

{% block body %}
    <h1>Collectible</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collectible.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collectible.name }}</td>
            </tr>
            <tr>
                <th>Properties</th>
                <td>{{ collectible.properties ? collectible.properties|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collectible_index') }}">back to list</a>

    <a href="{{ path('collectible_edit', {'id': collectible.id}) }}">edit</a>

    {{ include('collectible/_delete_form.html.twig') }}
{% endblock %}
