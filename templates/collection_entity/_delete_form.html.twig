<form method="post" action="{{ path('collection_entity_delete', {'id': collection_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collection_entity.id) }}">
    <button class="btn">Delete</button>
</form>
