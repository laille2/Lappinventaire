{% extends 'base.html.twig' %}

{% block title %}CollectionEntity{% endblock %}

{% block body %}
    <h1>CollectionEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection_entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collection_entity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collection_entity_index') }}">back to list</a>

    <a href="{{ path('collection_entity_edit', {'id': collection_entity.id}) }}">edit</a>

    {{ include('collection_entity/_delete_form.html.twig') }}
{% endblock %}
